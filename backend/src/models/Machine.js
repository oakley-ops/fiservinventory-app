class Machine {
    constructor(machine_id, name, model_number, serial_number) {
      this.machine_id = machine_id;
      this.name = name;
      this.model_number = model_number;
      this.serial_number = serial_number;
    }
    // You can add methods here, like updating machine details
  }
  
  module.exports = Machine;